ARTIFACT B (constituent-level diagnostics) — STATUS: NOT RUN

Reason: the frozen Gate A universe (AAPL, MSFT, GOOGL) cannot be executed.
The required market-cap endpoint GET /tiingo/fundamentals/GOOGL/daily returns
a persistent HTTP 400 plan-tier "DOW 30" entitlement restriction. The frozen
P5A-2 spec forbids substituting a ticker, changing the frozen universe,
deriving market cap from another endpoint, or weakening Gate A.

This is a persistent entitlement mismatch with the frozen Gate A universe, NOT
the separate temporary HTTP 429 rate-limit condition observed during live
recording. Gate A is BLOCKED; it is not PASS.

The only constituent diagnostics that exist are from the non-Gate-A,
entitlement-constrained partial run preserved verbatim (unmodified) at:
  partial_run_entitlement_limited_not_gate_a/artifact_b_constituent_diagnostics.{csv,json}
That run is explicitly NOT Gate A evidence: two-name set, now-lost live
fixtures. No normalized-weight column exists anywhere.
