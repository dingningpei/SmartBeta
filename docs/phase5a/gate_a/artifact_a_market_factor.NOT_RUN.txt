ARTIFACT A — Machine-readable factor output
STATUS: NOT RUN

Reason: the frozen Gate A universe (AAPL, MSFT, GOOGL) cannot be executed.
The required market-cap endpoint GET /tiingo/fundamentals/GOOGL/daily returns
a persistent HTTP 400 plan-tier "DOW 30" entitlement restriction, and the
frozen P5A-2 spec forbids substituting a ticker, changing the frozen universe,
deriving market cap from another endpoint, or weakening Gate A.

This is a persistent entitlement mismatch with the frozen Gate A universe, NOT
the separate temporary HTTP 429 rate-limit condition observed during live
recording. Gate A is BLOCKED; it is not PASS.

No Gate A artifact A is produced here. The only numeric factor output that
exists is the non-Gate-A, entitlement-constrained partial run preserved
verbatim (unmodified) at:
  partial_run_entitlement_limited_not_gate_a/artifact_a_market_factor.{csv,json}
That run is explicitly NOT Gate A evidence: it used a two-name set
(AAPL, MSFT) that is not the frozen universe, and the live fixtures it was
generated from were subsequently overwritten by HTTP 429 error bodies and must
not be reconstructed.
