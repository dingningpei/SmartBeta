# Non-Gate-A partial run (preserved verbatim)

**This directory is NOT Gate A evidence.**

These files are a two-name (AAPL, MSFT) **entitlement-constrained partial
run**, produced before the frozen Gate A universe was found to be fully
unexecutable and before the live fixtures it was generated from were overwritten
by HTTP 429 error bodies.

* Frozen Gate A requires the three-name universe **AAPL, MSFT, GOOGL**; this
  run used only **AAPL, MSFT** (and, after the trusted tradability screen, a
  single surviving in-window name, AAPL). It therefore **weakens Gate A** and
  must not be presented as a Gate A result.
* Its source live fixtures no longer exist and are **not reconstructed**.
* No ticker is substituted, no market cap is derived from another endpoint, and
  no missing fixture is fabricated.
* File contents are byte-for-byte identical to the files originally produced
  (SHA-256 preserved); only their location and status label changed.

It is retained solely so the exact numeric observations already obtained are
not erased. Artifacts A, B, and C for the actual Gate A are `NOT RUN`.
